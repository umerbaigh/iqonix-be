'use strict';

/**
 * must-have-product controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::must-have-product.must-have-product');
