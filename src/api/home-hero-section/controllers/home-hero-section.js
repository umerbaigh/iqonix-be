'use strict';

/**
 * home-hero-section controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::home-hero-section.home-hero-section');
